# ============================================================
# CODEGA AI - Faz 3 (LLM Motoru) bağımlılıkları
# ============================================================
# Faz 3'te aktif: LLM (llama-cpp-python), embedding (sentence-
# transformers + torch), RAG bellek (chromadb).
# ============================================================

# ---- Çekirdek ----
psutil>=5.9.0,<7.0.0
rich>=13.7.0,<14.0.0
tomli>=2.0.1; python_version < "3.11"

# ---- Backend ----
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic>=2.9.0,<3.0.0
python-multipart>=0.0.12,<1.0.0

# ---- Masaüstü pencere ----
pywebview>=5.3.0,<6.0.0

# ---- Yardımcı ----
httpx>=0.27.0,<1.0.0
packaging>=24.0,<25.0

# ============================================================
# Faz 3 (LLM + Embedding + RAG) - AKTİF
# ============================================================

# LLM motoru — GGUF tabanlı, CPU + CUDA destekli.
# CUDA için: pip install llama-cpp-python --extra-index-url \
#            https://abetlen.github.io/llama-cpp-python/whl/cu121
# CPU için: standart kurulum yeter.
llama-cpp-python>=0.3.2,<0.4.0

# Embedding — BGE-M3 için sentence-transformers
# torch kendi başına ~2GB; CUDA wheel ayrı:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
sentence-transformers>=3.0.0,<4.0.0

# sentence-transformers transitive deps — bazen PyInstaller'da
# otomatik bundle olmuyor, açıkça ekliyoruz
scipy>=1.10.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
torch>=2.2.0,<3.0.0

# Vektör veritabanı
chromadb>=0.5.0,<1.0.0

# HuggingFace model indirme (sentence-transformers'ın çekirdeği)
huggingface_hub>=0.25.0,<1.0.0

# ============================================================
# Faz 4 (Görsel Üretim) - AKTİF
# ============================================================
diffusers>=0.31.0,<1.0.0
accelerate>=1.0.0,<2.0.0
transformers>=4.45.0,<5.0.0
Pillow>=10.4.0,<12.0.0
safetensors>=0.4.5,<1.0.0

# ============================================================
# Faz 5 (Ses TTS+ASR) - AKTİF
# ============================================================
faster-whisper>=1.0.0,<2.0.0
TTS>=0.22.0,<0.23.0
soundfile>=0.12.0,<1.0.0

# Piper TTS opsiyonel (hafif TR-only)
# piper-tts>=1.2.0

# ============================================================
# Faz 6 (Video) - AKTİF
# ============================================================
# diffusers zaten Faz 4'te var, video için ek MP4 export için imageio:
imageio>=2.34.0,<3.0.0
imageio-ffmpeg>=0.5.0,<1.0.0

# ============================================================
# Faz 7 (Self-Learning) - YORUM SATIRINDA
# ============================================================
# peft>=0.13.0
# trl>=0.11.0
# bitsandbytes>=0.44.0

# ============================================================
# Faz 8 (Paketleme) - YORUM SATIRINDA
# ============================================================
# pyinstaller>=6.10.0

# ============================================================
# Faz 10 - İnternet Öğrenmesi (Web Learner)
# ============================================================
duckduckgo-search>=6.0.0,<8.0.0
beautifulsoup4>=4.12.0,<5.0.0
feedparser>=6.0.0,<7.0.0
lxml>=5.0.0

# ============================================================
# Faz 11 - Vision & Video Anlama
# ============================================================
Pillow>=10.0.0,<12.0.0          # Görüntü işleme (zaten transformers dep)
easyocr>=1.7.0                  # OCR - opsiyonel (pip install easyocr)
opencv-python-headless>=4.9.0   # Video frame çıkarma (headless = no GUI)
